{
  "name": "blog-backend",
  "version": "1.0.0",
  "description": "Node.js backend for online blog app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "if exist build rmdir /s /q build && mkdir build && copy server.js build && xcopy config build\\config /Y /I /E && xcopy models build\\models /Y /I /E && xcopy routes build\\routes /Y /I /E"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.4",
    "mongoose": "^7.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
